Shared utilities for Nagarro Ascent Platform.

Modules:
- nagarro_ascent_common.auth: JWT service and tokens

Install (editable) for local dev:
- Add to service requirements as '-e ../common' relative path, or pip install -e common
